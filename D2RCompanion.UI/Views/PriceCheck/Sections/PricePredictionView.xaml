<UserControl
    x:Class="D2RCompanion.UI.Views.PriceCheck.Sections.PricePredictionView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:D2RCompanion.UI.Views.PriceCheck.Sections"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!--  TITLE  -->
        <TextBlock
            Grid.Row="0"
            Margin="0,0,0,4"
            FontSize="12"
            Foreground="#888888"
            Text="PRICE PREDICTION" />

        <StackPanel
            Grid.Row="1"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Orientation="Horizontal">

            <TextBlock
                Margin="0,0,4,0"
                VerticalAlignment="Center"
                FontSize="18"
                Foreground="White"
                Text="{Binding PricePrediction, Converter={StaticResource IstPriceConverter}}"
                TextAlignment="Center" />


            <TextBlock
                Margin="4,0,0,0"
                VerticalAlignment="Center"
                FontSize="18"
                Foreground="LightGray"
                Text="{Binding PricePredictionHint}"
                TextAlignment="Center" />

        </StackPanel>

        <TextBlock
            Grid.Row="2"
            Margin="2"
            FontSize="14"
            Foreground="White"
            Text="{Binding PricePredictionConfidence, StringFormat='Confidence: {0:P0}'}"
            TextAlignment="Center" />

    </Grid>
</UserControl>
