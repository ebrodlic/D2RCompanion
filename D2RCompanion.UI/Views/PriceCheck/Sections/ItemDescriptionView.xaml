<UserControl
    x:Class="D2RCompanion.UI.Views.PriceCheck.Sections.ItemDescriptionView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:D2RCompanion.UI.Views.PriceCheck.Sections"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--  TITLE  -->
        <TextBlock
            Grid.Row="0"
            Margin="0,0,0,6"
            FontSize="12"
            Foreground="#888888"
            Text="ITEM DESCRIPTION" />

        <!--  CONTENT  -->
        <ScrollViewer
            Grid.Row="1"
            MaxHeight="250"
            VerticalScrollBarVisibility="Hidden">
            <ItemsControl ItemsSource="{Binding OcrText}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock
                            Margin="0,0,0,2"
                            FontSize="12"
                            Foreground="White"
                            Text="{Binding}" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

    </Grid>
</UserControl>
