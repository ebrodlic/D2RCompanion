<UserControl
    x:Class="D2RCompanion.UI.Controls.TraderieWebViewControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:D2RCompanion.UI.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>
        <!--  WebView2 control for displaying Traderie content  -->
        <wv2:WebView2
            Name="TraderieWebView"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch" />

        <!--  Floating Back Button  -->
        <!--<Button
            Name="BackButton"
            Width="100"
            Height="50"
            Margin="10"
            HorizontalAlignment="Left"
            VerticalAlignment="Top"
            Panel.ZIndex="10"
            Background="Gray"
            BorderBrush="DarkGray"
            BorderThickness="1"
            Click="BackButton_Click"
            Content="Back"
            FontWeight="Bold"
            Foreground="White" />-->
        <!--  High ZIndex to ensure it's on top  -->
    </Grid>
</UserControl>
